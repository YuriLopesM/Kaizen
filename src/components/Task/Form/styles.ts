import styled from 'styled-components';

export const Container = styled.form`
    display: flex;
    align-items: flex-end;
    gap: 2rem;

    input {
        width: 800px;
    }

    button {
        padding-inline: 1rem;
    }
`